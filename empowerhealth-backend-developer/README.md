# empowerhealth-backend

empowerhealth

sequelize-auto -h localhost -d empowerhealth -u root -x --dialect mysql -c config/config.json

npm install --save-dev sequelize-cli
npx sequelize-cli db:migrate

